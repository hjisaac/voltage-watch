{
  "name": "nuxt-starter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "api": "json-server --watch api/db.js --routes api/routes.json  --port 5000 --host 127.0.0.1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "isomorphic-fetch": "^3.0.0",
    "moment": "^2.29.4"
  },
  "devDependencies": {
    "core-js": "^3.30.2",
    "nuxt": "^2.16.3"
  }
}
